SELECT * FROM products_v1.category;

SET FOREIGN_KEY_CHECKS = 0;
TRUNCATE TABLE category;

INSERT INTO category (
    id,
    sector_title_id,
    sector_id,
    sub_sector_id,
    category_type,
    parent_category_id,
    category_name,
    description,
    info,
    note,
    system_note,
    image,
    status,
    is_active,
    is_enabled,
    created_at,
    updated_at
)
SELECT 
    UniqueId,
    1,
    1,
    1,

    CASE 
        WHEN primary_category_id IS NULL THEN 'primary'
        ELSE 'secondary'
    END,

    primary_category_id,
    CategoryName,
    description,
    Info,
    note,
    systemnote,
    Photo,
    'active',
    1,
    1,
    CreateDate,
    UpdateDate
FROM test_pay_db.mstrcategory;

SET FOREIGN_KEY_CHECKS = 1;

